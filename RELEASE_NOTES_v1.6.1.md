# ProtoSystem v1.6.1 - GUID Fixes & Project Setup Wizard

## Дата: 2026-01-09

## Исправленные проблемы

### 1. Конфликт GUID: EffectsManager.cs.meta vs EffectsManagerSystem.cs.meta

**Проблема:**
```
GUID [e81748ec822b2844697a7459ac9aa87b] conflicts between files
```

**Решение:**
- Регенерирован новый GUID: `a3f9b2c8d5e6f7a8b9c0d1e2f3a4b5c6`

### 2. Невалидный .meta файл: Tests/Editor.meta

**Решение:**
- Добавлен полный формат с GUID: `e5f67890a1b2c3d4e5f678901234abcd`

### 3. Assembly Definition - отсутствовало поле 'name'

**Проблема:**
```
Required property 'name' not set (*.asmdef)
```

**Решение:**
- Исправлена генерация JSON - теперь используется ручная сериализация
- Гарантируется наличие всех обязательных полей

## Новые возможности v1.6.1

### 🎯 ProjectSetupWizard - Мастер настройки проекта

Визард для первичной настройки с расширенными возможностями:

#### Настройки проекта:
- **Project Type**: Single Player / Multiplayer
- **Camera Type**: 2D / 3D
- **Render Pipeline**: Standard / URP / HDRP (автоопределение)

#### Автоматическое создание Bootstrap сцены:
- **Камера** с правильными настройками:
  - 2D: Orthographic, size 5, position (0,0,-10)
  - 3D: Perspective, FOV 60°, position (0,1,-10)
- **Освещение** (для 3D):
  - Standard: Intensity 1.0
  - URP: Intensity 1.0
  - HDRP: Intensity 130000 (physical units)
- **SystemInitializationManager**
- **EventSystem** для UI

#### Остальные возможности:
- Генерация структуры папок
- Assembly Definition с правильными references
- EventCategories с базовыми категориями
- UI спрайты и префабы
- Canvas структура с панелями

### 📝 Использование:

**Автоматически:**
```
Unity загружается → Диалог "Run Setup Wizard?"
```

**Вручную:**
```
Tools → ProtoSystem → Project Setup Wizard
```

**Сброс:**
```
Tools → ProtoSystem → Reset First Time Setup
```

## Компоненты

### Новые файлы:
- `ProjectSetupWizard.cs` - главное окно визарда
- `ProjectConfig.cs` - ScriptableObject для namespace
- `ProjectSetupDetector.cs` - автозапуск визарда
- `PROJECT_SETUP_WIZARD.md` - полная документация

### Изменённые файлы:
- `EffectsManagerSystem.cs.meta` - новый GUID
- `Tests/Editor.meta` - исправлен формат
- `package.json` - версия 1.6.1

## Инструкции по обновлению

### Для проектов с ошибками:

1. **Обновите до v1.6.1** через Package Manager
2. **Если конфликт GUID остался:**
   - Закройте Unity
   - Удалите `Library/` папку
   - Откройте проект (Unity пересоздаст кэш)

### Для новых проектов:

Используйте ProjectSetupWizard для быстрого старта:
- Автоматическое определение Render Pipeline
- Правильная настройка камеры и освещения
- Готовая структура проекта

## Детали реализации

### Автоопределение Render Pipeline:
```csharp
var pipeline = GraphicsSettings.currentRenderPipeline;
if (pipeline == null) return Standard;
if (pipeline.GetType().Name.Contains("Universal")) return URP;
if (pipeline.GetType().Name.Contains("HDRenderPipeline")) return HDRP;
```

### Настройка камеры для 2D:
- Orthographic projection
- Size: 5 units
- Position: (0, 0, -10)
- Clear flags: Solid Color

### Настройка камеры для 3D:
- Perspective projection
- FOV: 60°
- Position: (0, 1, -10)
- Clear flags зависят от pipeline

### Освещение для 3D:
- Directional Light
- Rotation: (50, -30, 0)
- Intensity зависит от pipeline (HDRP использует физические единицы)

## Совместимость

- Unity 2021.3+
- Standard / URP / HDRP pipelines
- Netcode for GameObjects 2.4.4+ (для Multiplayer)
- Обратная совместимость с v1.6.0

## Известные ограничения

- Освещение создаётся только для 3D проектов
- HDRP может требовать дополнительной настройки Volume Profile
- Для 2D с URP может потребоваться дополнительная настройка 2D Lights

## Roadmap

Планируется в следующих версиях:
- Поддержка 2D Lights для URP
- Настройка Volume Profiles для HDRP
- Шаблоны для разных жанров игр
- Интеграция с Asset Store пакетами

## Благодарности

Спасибо за фидбек по GUID конфликтам и Assembly Definition!
