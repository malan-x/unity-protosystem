# ProtoSystem v1.6.3 - Critical Assembly Definition Fix

## 📅 Дата: 2026-01-09

## 🔥 Критическое исправление

### Assembly Definition References - серые (не найденные) ссылки

**Проблема:**
```
The grayed out assembly references are missing
ProtoSystem → None (Assembly Definition Asset)
```

При использовании ProjectSetupWizard в **других проектах** создавался .asmdef с неправильным reference на ProtoSystem, что приводило к ошибкам компиляции:

```
error CS0246: The type or namespace name 'ProtoSystem' could not be found
```

**Причина:**
- Использовался reference `"ProtoSystem"` (неправильное имя)
- Правильное имя: `"ProtoSystem.Runtime"`
- Но имена могут конфликтовать между проектами
- **GUID постоянный и универсальный**

**Решение:**
Используется **GUID-based reference** для абсолютной стабильности:

```json
{
    "name": "MyGame",
    "references": [
        "GUID:f0916efc0967ba241b646b3544bfe86b",  // ProtoSystem.Runtime
        "Unity.TextMeshPro"
    ]
}
```

## ✅ Результат

**До исправления:**
- ❌ ProtoSystem reference серый
- ❌ Ошибки компиляции в EventCategories.cs
- ❌ Use GUIDs checkbox не помогает

**После исправления:**
- ✅ ProtoSystem.Runtime найден автоматически
- ✅ Компиляция без ошибок
- ✅ Работает в любом проекте

## 📦 Что изменено

### Файлы:

**ProjectSetupWizard.cs:**
```csharp
// Было:
references.Add("ProtoSystem");

// Стало:
references.Add("GUID:f0916efc0967ba241b646b3544bfe86b"); // ProtoSystem.Runtime
```

### Документация:
- `PROJECT_SETUP_WIZARD.md` - обновлена информация о references
- `RELEASE_NOTES_v1.6.1.md` - добавлено описание проблемы
- `SETUP_WIZARD_QUICK_GUIDE.md` - упоминание GUID-based подхода

## 🎯 Для пользователей

### Если у вас уже есть проект с серым ProtoSystem:

**Вариант 1 - Пересоздать .asmdef:**
1. Удалить `Assets/[YourProject]/Scripts/[Namespace].asmdef`
2. Запустить `Tools → ProtoSystem → Project Setup Wizard`
3. Execute → "Generate Assembly Definition"

**Вариант 2 - Исправить вручную:**
Открыть `*.asmdef` в текстовом редакторе:
```json
{
    "references": [
        "GUID:f0916efc0967ba241b646b3544bfe86b",  // Вместо "ProtoSystem"
        "Unity.TextMeshPro"
    ]
}
```

**Вариант 3 - Через Inspector:**
1. Открыть .asmdef в Inspector
2. Assembly Definition References → Use GUIDs ✅
3. ProtoSystem → выбрать из списка
4. Apply

### Для новых проектов:

Просто используйте обновленный визард - всё будет работать из коробки! 🎉

## 📚 Технические детали

### Почему GUID-based references лучше?

1. **Универсальность**
   - GUID одинаковый во всех проектах
   - Имя может конфликтовать

2. **Стабильность**
   - GUID не меняется при переименовании
   - Безопасно для рефакторинга

3. **Package-friendly**
   - Рекомендуется Unity для пакетов
   - Избегает конфликтов версий

### GUID ProtoSystem пакетов:

```
ProtoSystem.Runtime:  f0916efc0967ba241b646b3544bfe86b
ProtoSystem.Editor:   (из Editor.asmdef)
ProtoSystem.Effects:  (из Effects.asmdef)
```

## 🔄 История версий

**v1.6.3** - Исправление Assembly Definition References (GUID)
**v1.6.2** - (пропущена)
**v1.6.1** - ProjectSetupWizard, Camera/Lighting, asmdef fixes
**v1.6.0** - Effects, UI, Network systems

## 🆘 Поддержка

Если после обновления проблема осталась:
1. Reimport пакет ProtoSystem
2. Перезапустить Unity
3. Очистить Library/ (крайний случай)

## ✨ Бонус

ProjectSetupWizard теперь создаёт **идеальный** .asmdef с первого раза:
- ✅ Правильные GUID-based references
- ✅ Все обязательные поля
- ✅ Правильный JSON формат
- ✅ Работает везде

---

Спасибо за использование ProtoSystem! 🚀
