# ⚡ Quick Fix - Визард не создал правильный EventBus

## Проблема

После запуска визарда v1.6.4-1.6.5:
- ❌ Создался файл `{Namespace}EventBus.cs` (неправильный)
- ❌ UI панель показывает "EventBus файл не найден"
- ❌ Ошибка компиляции `EventCategory not found`

## Решение (2 минуты)

### Шаг 1: Удалить старый файл
```
Assets/LastConvoy/Scripts/Events/LastConvoyEventBus.cs → DELETE
```

### Шаг 2: Исправить Assembly Definition

**Откройте:** `Assets/LastConvoy/Scripts/LastConvoy.asmdef`

**Замените:**
```json
"references": [
    "ProtoSystem",  ← удалить
```

**На:**
```json
"references": [
    "GUID:f0916efc0967ba241b646b3544bfe86b",  // ProtoSystem.Runtime
```

### Шаг 3: Пересоздать через визард

1. `Tools → ProtoSystem → Project Setup Wizard`
2. Кнопка **Reset Progress** (внизу окна)
3. Кнопка **Execute All Pending**

## ✅ Результат

После исправления:
- ✅ Создан файл `EventIds.LastConvoy.cs`
- ✅ Namespace сохранён в EditorPrefs
- ✅ UI панель видит файл
- ✅ Нет ошибок компиляции

## 📋 Проверка

**UI панель "EventBus проекта" должна показывать:**
```
Namespace проекта: LastConvoy
Файл: Assets/LastConvoy/Scripts/Events/EventIds.LastConvoy.cs
События: 0
Категории: 0
```

## 🎯 Если всё ещё не работает

### Проблема: Ошибка `EventCategory not found`

**Причина:** Assembly Definition не имеет GUID reference

**Решение:** См. Шаг 2 выше

### Проблема: UI панель не видит файл

**Причина:** Namespace не сохранён в EditorPrefs

**Решение:** 
1. Выбрать SystemInitializationManager
2. В панели "EventBus проекта" ввести namespace
3. Нажать "Создать EventBus файл"

### Проблема: Визард создаёт неправильный файл

**Причина:** Старая версия пакета (< v1.6.6)

**Решение:**
1. Package Manager → ProtoSystem Core
2. Update to v1.6.6+
3. Перезапустить Unity
4. Повторить все шаги

---

**Обновитесь до v1.6.6 для правильной интеграции!** 🚀
