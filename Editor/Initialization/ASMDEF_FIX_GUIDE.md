# 🔧 Быстрое исправление серого ProtoSystem reference

## Проблема
```
Assembly Definition References:
ProtoSystem → None (Assembly Definition Asset) [серый текст]
```

## ⚡ Быстрое решение (30 секунд)

### Способ 1: Через Inspector
1. Выбрать `.asmdef` файл в Project
2. В Inspector → Assembly Definition References
3. Поставить галку **Use GUIDs** ✅
4. В выпадающем списке ProtoSystem выбрать **ProtoSystem.Runtime**
5. Apply

### Способ 2: Текстовый редактор (самый быстрый)
Открыть ваш `.asmdef` файл и заменить:
```json
"references": [
    "ProtoSystem",          ← удалить эту строку
```

на:

```json
"references": [
    "GUID:f0916efc0967ba241b646b3544bfe86b",  // ProtoSystem.Runtime
```

Сохранить, вернуться в Unity.

### Способ 3: Пересоздать через визард
1. Удалить ваш `.asmdef` файл
2. `Tools → ProtoSystem → Project Setup Wizard`
3. Execute → "Generate Assembly Definition"

## ✅ Проверка

После исправления должно быть:
- ✅ ProtoSystem.Runtime показывается **нормальным** шрифтом
- ✅ Нет ошибок компиляции
- ✅ EventCategories.cs компилируется

## 📝 Пример правильного .asmdef

```json
{
    "name": "MyGame",
    "rootNamespace": "MyGame",
    "references": [
        "GUID:f0916efc0967ba241b646b3544bfe86b",
        "Unity.TextMeshPro"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
```

## ❓ Если не помогло

1. Reimport пакет ProtoSystem:
   - Правый клик на пакет → Reimport

2. Перезапустить Unity

3. В крайнем случае:
   - Закрыть Unity
   - Удалить папку `Library/`
   - Открыть проект

## 🎯 Почему это важно?

GUID-based references:
- ✅ Работают везде
- ✅ Не конфликтуют
- ✅ Рекомендуются Unity для пакетов
- ✅ Не ломаются при обновлении

Name-based references:
- ❌ Могут не работать в других проектах
- ❌ Зависят от порядка загрузки
- ❌ Конфликтуют при дубликатах

---

**Обновите до ProtoSystem v1.6.3** - исправление уже включено в визард!
